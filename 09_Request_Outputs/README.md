# Request Outputs

Request-specific mappings generated from the Current Approved Master plus the supplied intake/request data.

These outputs are delivery artifacts, not canonical Masters.

A request-specific difference may become a Learning Candidate or Master Change Candidate, but it must not update the Master automatically.
